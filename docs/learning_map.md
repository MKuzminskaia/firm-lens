## 2026-02-08 (Day 1)
- Git: init/add/commit/push, remote (GitHub), базовый .gitignore
- Python/Tools: venv (попытки), pip, понимание что такое requirements.txt
- Streamlit: st.title/st.header/st.write, st.file_uploader, st.dataframe
- Pandas: pd.read_csv, df.head()
Result: minimal app — upload CSV -> preview first rows

## 2026-02-09 (Day 2)
- Python: if/elif/else, функции и return, dict + items() + get()
- Pandas: DataFrame, df.shape/df.columns, Series.apply(), sort_values(), reset_index()
- Streamlit: st.selectbox, st.success/info, st.download_button
Result: upload CSV -> choose column -> score rows -> sort by score -> download result CSV